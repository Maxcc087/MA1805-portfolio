In prompt 3, I was required to use arrays, loops, and conditionals to explore repetition. So I just think of loading screen, where the tiny circles just spinning in circles endlessly. I used array to store the size of each circles and use "for" to make loop through each circle. Also with help from youtube and different examples I use "TWO_PI" to make a perfect circular loop. Last I use the if statement to help change the size of the circles to make it not that boring. I think it just shows how helpless it is when you just stare at it and nothing is really happening.